
<!DOCTYPE html>
<html lang="uz">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <title>Edit City</title>
    <style>
      body {
        font-family: "Jost", sans-serif;
        background: linear-gradient(to bottom, #0f0c29, #302b63, #24243e);
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        margin: 0;
      }
      .main {
        width: 350px;
        background: rgba(255, 255, 255, 0.8);
        padding: 20px;
        border-radius: 10px;
        box-shadow: 5px 5px 20px rgba(0, 0, 0, 0.2);
      }
      .radio-container {
        display: flex;
        justify-content: space-around;
        margin: 20px 0;
      }
      input[type="text"],
      input[type="number"] {
        width: 100%;
        margin: 10px 0;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
      }
      button {
        width: 100%;
        padding: 10px;
        background-color: #573b8a;
        color: #fff;
        border: none;
        border-radius: 5px;
        cursor: pointer;
      }
      div.form-check-inline {
        line-height: 1.5rem;
        display: inline-flex;
      }
      label {
        line-height: 1.5rem;
        margin-left: 5px;
      }
      input[type="radio"] {
        line-height: 1.5rem;
        margin: auto;
      }
      button:hover {
        background-color: #6d44b8;
      }
    </style>
  </head>
  <body>
    <div class="main">
        <div class="signup">
                <form method="post" action="/country-edit/<%= numericId %>">

                <label for="chk" aria-hidden="true">Editing</label>
                <input type="text" name="name" placeholder="Name" required />

                <div class="input-container">
                    <input
                        type="number"
                        name="population"
                        placeholder="Population"
                        required
                    />
                    <input
                        type="number"
                        name="regions_count"
                        placeholder="Regions count (for cities!)"
                    />
                </div>

                <button type="submit">SEND</button>
            </form>
        </div>

    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
